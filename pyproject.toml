[build-system]
requires = ["flit_core>=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "opt-prop"
version = "0.1.0"
description = "Turbulent media project"

# This is required for imports to work?
[tool.flit.module]
name = "optical_functions"